ToDo list

Items to do:

- quiet option fix
- package: verbosity fix
- torepo: no freight output, only summary
- torepo process: output or ... ?
- Test set maken?
- Test error in version command
- logging?
- all error messages to stderr
v output for error messages
- seperate out all sanity checks
v torepo optie verwijderen
- change argument parsing
- change over to new process flow
- create different levels of output
- instance should be change to a recipe name modifier
v check recipes into svn
v test all recipes
v daily build all recipes? -> auto build flag oid?
- nginx recipe tegen git https://github.com/nginx/nginx
- verify recipe dependancies
- harden output of version command (ook voor niet bestaand pakket!)
- script naar github
v script integreren
- example script
v update documentation for sudo rights
- create readme.md for publication on github
x remote build to zones
v set buildrc defaults if no file found or mandatory option not found
- auto build configuration entry in recipies
- auto build option
- check if directories exist
- all error to stderr
- revisit all variable names for consistency
v check required commands available
- fixed setuid bits
- define distro names to insert package to
v svn import
- hardening of input
v review all return code checks
- BASEDIR in recipe zetten ipv global config?
v all functions local variables
- review config section names
- auto install compile dependancies if configured and sudo available
- packaging add capability for running scripts for a package.
- check torepo=n
- recipe with "package ready" needs fixing
- change dpkg substitute parameters with B_
- compressed caching support (save diskspace)

Done:

v create function for git repo version
v clean up function names
v Improve help output
v Install/remove build dependancies
v check recipe section exists
v ID en B_ARCH scheiden
v pkg build functies splitsen
v to repository functie
v start documentation
v external configuration file
v require per package type
v rm -rf build dir after error during build
v external configuration file
v fix getpart function change to recipe_section_get

